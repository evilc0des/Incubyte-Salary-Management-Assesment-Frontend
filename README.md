# Frontend

Next.js App Router starter using TypeScript.

## Run locally

1. Install dependencies:

   ```powershell
   npm install
   ```

2. Copy `.env.example` to `.env`.
3. Start the app:

   ```powershell
   npm run dev
   ```

